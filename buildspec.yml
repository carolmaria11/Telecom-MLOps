version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - pip install -r requirments.txt
  build:
    commands:
      - python train.py
artifacts:
  files:
    - telecom_tower_model.pkl
