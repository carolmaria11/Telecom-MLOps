version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - pip install -r requirments.txt
  build:
    commands:
      - python MLOps_Lab1.py
artifacts:
  files:
    - telecom_tower_model.pkl
